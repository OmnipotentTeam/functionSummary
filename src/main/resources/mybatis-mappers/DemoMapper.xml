<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhxx.service.szxt.mapper.DemoMapper">

	<select id="queryAll" resultType="com.zhxx.service.szxt.entity.Demosel">
		select * from Demosel t
	</select>
   <select id="querycount" resultType="int">
		select count(*) from Demosel t
	</select>
	
</mapper>
